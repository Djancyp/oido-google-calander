name = "list-events"
description = "List upcoming events from the calendar."

prompt = """
List upcoming events from the calendar using the list_events tool.
Show the event ID, summary, start time, and end time for each event.
"""

[parameters]
max_results = { type = "number", description = "Maximum number of events to return (default: 20)", default = 20 }
