name = "get-event"
description = "Get full details of a specific event by its ID."

prompt = """
Get full details of event with ID {{event_id}} using the get_event tool.
Show summary, description, start, end, location, status, creator, and attendees.
"""

[parameters]
event_id = { type = "string", description = "ID of the event to retrieve", required = true }
